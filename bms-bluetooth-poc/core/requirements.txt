# 藍牙通訊
bleak>=0.21.0        # BLE (低功耗藍牙) 支援
pyserial>=3.5        # 串口通訊 (如果藍牙模組用串口模式)

# 終端顯示美化
rich>=13.0           # 豐富的終端輸出
colorama>=0.4.6      # 跨平台終端顏色

# 數據處理
pandas>=2.0          # 數據處理和分析
numpy>=1.24          # 數值計算

# 可選: 圖表顯示
matplotlib>=3.7      # 簡單圖表繪製 (可選)

# 開發工具
python-dotenv>=1.0   # 環境變數管理