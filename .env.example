# ===========================================
# 🔋 DALY BMS 監控系統 - 環境變數範例
# ===========================================
# 複製此檔案為 .env 並填入實際值

# FastAPI 應用設定
DEBUG=true
HOST=0.0.0.0
PORT=8000
WS_PORT=8001

# 資料庫配置 (PostgreSQL)
# 格式: postgresql+asyncpg://用戶名:密碼@主機:端口/資料庫名
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/battery_monitor
POSTGRES_DB=battery_monitor
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Redis 緩存配置
REDIS_URL=redis://localhost:6379
REDIS_HOST=localhost
REDIS_PORT=6379

# MQTT 配置
# 注意: 使用 1884 端口避免與系統 MQTT 衝突
MQTT_BROKER_URL=mqtt://localhost:1884
MQTT_CLIENT_ID=fastapi-battery-monitor
MQTT_TOPIC_PREFIX=battery

# BMS 藍牙配置
# TODO: 修改為您的 BMS 設備 MAC 地址
BMS_MAC_ADDRESS=41:18:12:01:37:71
BMS_READ_INTERVAL=30

# 日誌配置
# 可選值: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 前端配置 (預留)
REACT_APP_API_URL=http://localhost:8000
REACT_APP_WS_URL=ws://localhost:8001

# Docker Compose 專案名稱
COMPOSE_PROJECT_NAME=battery-monitor

# ===========================================
# 安全注意事項:
# 1. 請勿將此檔案加入版本控制
# 2. 生產環境請使用強密碼
# 3. 限制資料庫存取權限
# 4. 定期更換密碼和金鑰
# ===========================================